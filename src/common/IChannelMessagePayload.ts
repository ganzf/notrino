import IChannel from "./IChannel";

type IChannelMessagePayload = any;

// For now, message payload is `any` but it could become a more
// strongly typed variable.

export default IChannelMessagePayload;